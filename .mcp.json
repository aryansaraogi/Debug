{
  "mcpServers": {
    "devops-filesystem": {
      "command": "${DEVOPS_MCP_PYTHON:-python}",
      "args": [
        "-m",
        "devops_mcp.servers.filesystem"
      ]
    },
    "devops-git": {
      "command": "${DEVOPS_MCP_PYTHON:-python}",
      "args": [
        "-m",
        "devops_mcp.servers.git"
      ]
    },
    "devops-docker": {
      "command": "${DEVOPS_MCP_PYTHON:-python}",
      "args": [
        "-m",
        "devops_mcp.servers.docker"
      ]
    },
    "devops-logs": {
      "command": "${DEVOPS_MCP_PYTHON:-python}",
      "args": [
        "-m",
        "devops_mcp.servers.logs"
      ]
    },
    "devops-actions": {
      "command": "${DEVOPS_MCP_PYTHON:-python}",
      "args": [
        "-m",
        "devops_mcp.servers.actions"
      ]
    }
  }
}
